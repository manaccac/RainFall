void    v() {
    char    s[536];
    int     i;

    fgets(s, 512,stdin);
    printf(s);
    if (i == 64) {
        fwrite(le machin osef);
        system("/bin/sh");
    }
}

void    main() {
    v();
    return;
}